import colors from "./colors";

export const darkTheme = {
    bgColor: colors.mainColor,
    layoutColor: colors.darkBgColor,
    fontColor: "white",
    iconColor: colors.subColor.beige,
};

export const lightTheme = {
    bgColor: colors.mainColor,
    layoutColor: colors.bgColor,
    fontColor: "black",
    iconColor: colors.subColor.beige,
};
